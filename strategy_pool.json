{
  "2bbb59ca": {
    "id": "2bbb59ca",
    "code": "def calculate_dynamic_signals(state, history):\n    symbol = 'BTC/USDT'\n    rsi_2 = state.signals.get(f'{symbol}_rsi_2', 50.0)\n    ema_20 = state.signals.get(f'{symbol}_ema_20', 0.0)\n\n    bars = history.get(symbol, [])\n    last_price = 0.0\n    for bar in reversed(bars):\n        for attr in ('close', 'last', 'bid', 'ask'):\n            value = getattr(bar, attr, None)\n            if value not in (None, 0):\n                last_price = float(value)\n                break\n        if last_price:\n            break\n\n    near_ema = True\n    if ema_20 > 0 and last_price > 0:\n        near_ema = abs(last_price - ema_20) / ema_20 <= 0.006\n\n    if rsi_2 <= 35.0 and near_ema:\n        return {'gemini_buy': 1.0}\n    if rsi_2 >= 65.0 and near_ema:\n        return {'gemini_sell': 1.0}\n    return {}",
    "name": "RSI2-EMA20-Pullback-Scalper",
    "explanation": "This child widens the parent's RSI-2 trigger band from 20/80 to 35/65 and adds an EMA-20 proximity check to scalp faster BTC mean-reversion pullbacks. The wider band should generate more frequent hyper-active scalping signals while keeping entries tied to the current micro-trend.",
    "parent_id": null
  },
  "7392b0b6": {
    "id": "7392b0b6",
    "code": "def calculate_dynamic_signals(state, history):\n    symbol = 'BTC/USDT'\n    state_signals = getattr(state, 'signals', {})\n    history = history or {}\n\n    def to_float(value, default=0.0):\n        try:\n            if value is None:\n                return default\n            return float(value)\n        except (TypeError, ValueError):\n            return default\n\n    def pick_value(obj, names):\n        if obj is None:\n            return None\n        if isinstance(obj, dict):\n            for name in names:\n                if name in obj:\n                    value = obj[name]\n                    if isinstance(value, dict):\n                        value = value.get('price') or value.get('last') or value.get('vwap')\n                    if value is not None:\n                        try:\n                            return float(value)\n                        except (TypeError, ValueError):\n                            pass\n        else:\n            for name in names:\n                value = getattr(obj, name, None)\n                if isinstance(value, dict):\n                    value = value.get('price') or value.get('last') or value.get('vwap')\n                if value is not None:\n                    try:\n                        return float(value)\n                    except (TypeError, ValueError):\n                        pass\n        return None\n\n    rsi_2 = to_float(state_signals.get(symbol + '_rsi_2', 50.0), 50.0)\n    rsi_14 = to_float(state_signals.get(symbol + '_rsi_14', 50.0), 50.0)\n    ema_20 = to_float(state_signals.get(symbol + '_ema_20', 0.0), 0.0)\n\n    bars = history.get(symbol, [])\n    prices = []\n    for ticker in bars[-12:]:\n        price = pick_value(ticker, ('close', 'last', 'vwap', 'lastPrice', 'price'))\n        if price is not None:\n            prices.append(price)\n\n    close = prices[-1] if prices else ema_20\n\n    bid = pick_value(bars[-1], ('bid', 'bidPrice')) if bars else None\n    ask = pick_value(bars[-1], ('ask', 'askPrice')) if bars else None\n    mid = (bid + ask) / 2.0 if bid and ask and bid > 0 and ask > 0 else 0.0\n    price_side = (close - mid) / mid if mid > 0 and close > 0 else 0.0\n\n    if len(prices) >= 3:\n        micro_mom = close - (sum(prices[-3:]) / 3.0)\n        window = prices[-6:]\n        vol = (max(window) - min(window)) / max(sum(window) / len(window), 1e-9)\n    else:\n        micro_mom = 0.0\n        vol = 0.0001\n    vol = max(vol, 0.0001)\n\n    buy_threshold = 35.0\n    sell_threshold = 65.0\n\n    if ema_20 > 0.0 and close > 0.0:\n        ema_dev = (close - ema_20) / ema_20\n        if ema_dev > 0.0010:\n            buy_threshold = 40.0\n            sell_threshold = 60.0\n        elif ema_dev < -0.0010:\n            buy_threshold = 30.0\n            sell_threshold = 70.0\n        else:\n            ema_dev = 0.0\n    else:\n        ema_dev = 0.0\n\n    if price_side > 0.00015:\n        buy_threshold = max(buy_threshold, 37.0)\n        sell_threshold = max(sell_threshold, 63.0)\n    elif price_side < -0.00015:\n        buy_threshold = min(buy_threshold, 33.0)\n        sell_threshold = min(sell_threshold, 67.0)\n\n    if micro_mom < -vol * close * 0.25:\n        buy_threshold = min(buy_threshold, 38.0)\n    elif micro_mom > vol * close * 0.25:\n        sell_threshold = max(sell_threshold, 62.0)\n\n    signals = {}\n    if rsi_2 < 18.0 or (rsi_2 < buy_threshold and rsi_14 < 48.0 and (ema_20 <= 0.0 or close >= ema_20 * 0.9995)):\n        signals['gemini_buy'] = 1.0\n    elif rsi_2 > 82.0 or (rsi_2 > sell_threshold and rsi_14 > 52.0 and (ema_20 <= 0.0 or close <= ema_20 * 1.0005)):\n        signals['gemini_sell'] = 1.0\n\n    return signals",
    "name": "EMA-Biased RSI2 Scalper",
    "explanation": "This child mutates the parent by using RSI-2 with adaptive 35/65 trigger bands around the 20-EMA regime, recent micro-momentum, and bid/ask pressure. It stays hyper-active by buying shallow oversold pullbacks and selling shallow overbought rallies in BTC/USDT.",
    "parent_id": null
  },
  "d2b75b70": {
    "id": "d2b75b70",
    "code": "def calculate_dynamic_signals(state, history):\n    symbol = 'BTC/USDT'\n\n    def _num(value, default):\n        try:\n            return float(value)\n        except (TypeError, ValueError):\n            return default\n\n    signals_map = getattr(state, 'signals', {}) or {}\n    rsi_2 = _num(signals_map.get(f'{symbol}_rsi_2', 50.0), 50.0)\n    rsi_14 = _num(signals_map.get(f'{symbol}_rsi_14', 50.0), 50.0)\n    ema_20 = _num(signals_map.get(f'{symbol}_ema_20', 0.0), 0.0)\n\n    candles = []\n    for t in reversed(history.get(symbol, [])):\n        close = getattr(t, 'close', None)\n        if close is None:\n            close = getattr(t, 'last', None)\n        try:\n            close = float(close)\n        except (TypeError, ValueError):\n            continue\n        volume = _num(getattr(t, 'volume', None), 0.0)\n        if close > 0.0:\n            candles.append((close, volume if volume > 0.0 else 0.0))\n        if len(candles) >= 12:\n            break\n\n    prices = [c for c, v in candles]\n    volumes = [v for c, v in candles]\n\n    signals = {}\n    if len(prices) >= 4:\n        new_price = prices[0]\n        prev_price = prices[1] if len(prices) > 1 else new_price\n        old_price = prices[-1]\n        micro_mom = (new_price - old_price) / old_price if old_price > 0.0 else 0.0\n        tick_delta = new_price - prev_price\n        fast_mean = sum(prices[:3]) / 3.0\n        mid_mean = sum(prices[3:7]) / 4.0\n        wide_n = min(len(prices), 10)\n        wide_mean = sum(prices[:wide_n]) / float(wide_n)\n        ema_spread = (new_price - ema_20) / ema_20 if ema_20 > 0.0 else 0.0\n        look = prices[:min(len(prices), 8)]\n        lo = min(look)\n        hi = max(look)\n        range_pos = (new_price - lo) / (hi - lo) if hi > lo else 0.5\n\n        up_vol = 0.0\n        down_vol = 0.0\n        for i in range(min(len(prices) - 1, 7)):\n            if prices[i] >= prices[i + 1]:\n                up_vol += volumes[i]\n            else:\n                down_vol += volumes[i]\n        vol_total = up_vol + down_vol\n        volume_imbalance = (up_vol - down_vol) / vol_total if vol_total > 0.0 else 0.0\n\n        buy_score = 0\n        sell_score = 0\n\n        if rsi_2 < 42.0:\n            buy_score += 2\n        if rsi_2 > 58.0:\n            sell_score += 2\n        if rsi_14 < 56.0:\n            buy_score += 1\n        if rsi_14 > 44.0:\n            sell_score += 1\n        if ema_20 > 0.0 and ema_spread < -0.00025:\n            buy_score += 1\n        if ema_20 > 0.0 and ema_spread > 0.00025:\n            sell_score += 1\n        if fast_mean < wide_mean:\n            buy_score += 1\n        if fast_mean > wide_mean:\n            sell_score += 1\n        if range_pos < 0.30:\n            buy_score += 1\n        if range_pos > 0.70:\n            sell_score += 1\n        if tick_delta > 0.0 and micro_mom < 0.00025:\n            buy_score += 1\n        if tick_delta < 0.0 and micro_mom > -0.00025:\n            sell_score += 1\n        if volume_imbalance > 0.08:\n            buy_score += 1\n        if volume_imbalance < -0.08:\n            sell_score += 1\n\n        if buy_score >= 2 and buy_score > sell_score:\n            signals['gemini_buy'] = 1.0\n        elif sell_score >= 2 and sell_score > buy_score:\n            signals['gemini_sell'] = 1.0\n        elif rsi_2 < 30.0 and rsi_14 < 68.0 and sell_score <= buy_score:\n            signals['gemini_buy'] = 1.0\n        elif rsi_2 > 70.0 and rsi_14 > 32.0 and buy_score <= sell_score:\n            signals['gemini_sell'] = 1.0\n        elif fast_mean < mid_mean and range_pos < 0.35 and rsi_2 < 55.0 and sell_score <= buy_score:\n            signals['gemini_buy'] = 1.0\n        elif fast_mean > mid_mean and range_pos > 0.65 and rsi_2 > 45.0 and buy_score <= sell_score:\n            signals['gemini_sell'] = 1.0\n        else:\n            if rsi_2 < 42.0 and sell_score <= buy_score:\n                signals['gemini_buy'] = 1.0\n            elif rsi_2 > 58.0 and buy_score <= sell_score:\n                signals['gemini_sell'] = 1.0\n    else:\n        if rsi_2 < 42.0:\n            signals['gemini_buy'] = 1.0\n        elif rsi_2 > 58.0:\n            signals['gemini_sell'] = 1.0\n\n    return signals",
    "name": "Wide-Band Micro-Range Scalper",
    "explanation": "This child widens the RSI trigger bands and adds an 8-tick range-position filter plus optional volume-imbalance confirmation, so it fires more often on BTC micro mean-reversion. It is designed to scalp quick oscillations instead of holding through larger trends.",
    "parent_id": "4b972daa"
  },
  "76ccf4d8": {
    "id": "76ccf4d8",
    "code": "def calculate_dynamic_signals(state, history):\n    symbol = 'BTC/USDT'\n    state_signals = getattr(state, 'signals', {}) or {}\n    history = history or {}\n\n    def to_float(value, default=0.0):\n        try:\n            if value is None:\n                return default\n            return float(value)\n        except (TypeError, ValueError):\n            return default\n\n    def pick_value(obj, names):\n        if obj is None:\n            return None\n        if isinstance(obj, dict):\n            for name in names:\n                if name in obj:\n                    value = obj[name]\n                    if isinstance(value, dict):\n                        value = value.get('price') or value.get('last') or value.get('vwap')\n                    if value is not None:\n                        try:\n                            return float(value)\n                        except (TypeError, ValueError):\n                            pass\n        else:\n            for name in names:\n                value = getattr(obj, name, None)\n                if isinstance(value, dict):\n                    value = value.get('price') or value.get('last') or value.get('vwap')\n                if value is not None:\n                    try:\n                        return float(value)\n                    except (TypeError, ValueError):\n                        pass\n        return None\n\n    rsi_2 = to_float(state_signals.get(symbol + '_rsi_2', 50.0), 50.0)\n    rsi_14 = to_float(state_signals.get(symbol + '_rsi_14', 50.0), 50.0)\n    ema_20 = to_float(state_signals.get(symbol + '_ema_20', 0.0), 0.0)\n\n    bars = history.get(symbol, [])\n    prices = []\n    mids = []\n    sides = []\n    for ticker in bars[-24:]:\n        price = pick_value(ticker, ('close', 'last', 'vwap', 'lastPrice', 'price'))\n        if price is not None and price > 0.0:\n            prices.append(price)\n        bid = pick_value(ticker, ('bid', 'bidPrice'))\n        ask = pick_value(ticker, ('ask', 'askPrice'))\n        if bid is not None and ask is not None and bid > 0.0 and ask > 0.0:\n            mid = (bid + ask) / 2.0\n            mids.append(mid)\n            ref = price if price is not None else mid\n            sides.append(1.0 if ref >= mid else -1.0)\n\n    close = prices[-1] if prices else ema_20\n    mid = mids[-1] if mids else close\n\n    if len(prices) >= 4:\n        mom1 = close - prices[-2]\n        window4 = prices[-4:]\n        window8 = prices[-8:]\n        window12 = prices[-12:]\n        mom3 = close - (sum(window4) / float(len(window4)))\n        mom7 = close - (sum(window8) / float(len(window8)))\n        base = max(sum(window12) / float(len(window12)), 1e-9)\n        vol = (max(window12) - min(window12)) / base\n    else:\n        mom1 = 0.0\n        mom3 = 0.0\n        mom7 = 0.0\n        vol = 0.00008\n    vol = max(vol, 0.00008)\n\n    order_flow = sum(sides[-10:]) / max(len(sides[-10:]), 1)\n    bid = pick_value(bars[-1], ('bid', 'bidPrice')) if bars else None\n    ask = pick_value(bars[-1], ('ask', 'askPrice')) if bars else None\n    spread = 0.0\n    if bid is not None and ask is not None and bid > 0.0 and ask > 0.0:\n        spread = (ask - bid) / ((bid + ask) / 2.0)\n    price_side = (close - mid) / mid if mid > 0.0 and close > 0.0 else 0.0\n\n    buy_rsi2 = 27.0\n    sell_rsi2 = 73.0\n    buy_rsi14 = 46.5\n    sell_rsi14 = 53.5\n\n    if ema_20 > 0.0 and close > 0.0:\n        ema_dev = (close - ema_20) / ema_20\n        if ema_dev > 0.0015:\n            buy_rsi2 += 4.0\n            sell_rsi2 -= 4.0\n        elif ema_dev < -0.0015:\n            buy_rsi2 -= 4.0\n            sell_rsi2 += 4.0\n\n    if mom3 < -(vol * close * 0.20):\n        buy_rsi2 += 6.0\n        sell_rsi2 += 2.0\n    elif mom3 > vol * close * 0.20:\n        buy_rsi2 -= 2.0\n        sell_rsi2 -= 6.0\n\n    if mom7 < 0.0:\n        buy_rsi2 += 2.0\n    else:\n        sell_rsi2 += 2.0\n\n    if order_flow < -0.10:\n        buy_rsi2 += 5.0\n        sell_rsi2 += 3.0\n    elif order_flow > 0.10:\n        buy_rsi2 -= 3.0\n        sell_rsi2 -= 5.0\n\n    if price_side < -(spread * 0.25):\n        buy_rsi2 += 4.0\n    elif price_side > spread * 0.25:\n        sell_rsi2 -= 4.0\n\n    buy_rsi2 = min(max(buy_rsi2, 14.0), 43.0)\n    sell_rsi2 = min(max(sell_rsi2, 57.0), 86.0)\n\n    buy_score = 0.0\n    sell_score = 0.0\n\n    if rsi_2 < 14.0:\n        buy_score += 3.0\n    if rsi_2 > 86.0:\n        sell_score += 3.0\n    if rsi_2 < buy_rsi2 and rsi_14 < buy_rsi14:\n        buy_score += 2.0\n    if rsi_2 > sell_rsi2 and rsi_14 > sell_rsi14:\n        sell_score += 2.0\n    if mom1 > vol * close * 0.08 and rsi_2 < 55.0 and order_flow <= 0.25:\n        buy_score += 1.0\n    if mom1 < -(vol * close * 0.08) and rsi_2 > 45.0 and order_flow >= -0.25:\n        sell_score += 1.0\n    if mom3 < -(vol * close * 0.12) and rsi_2 < 50.0:\n        buy_score += 1.0\n    if mom3 > vol * close * 0.12 and rsi_2 > 50.0:\n        sell_score += 1.0\n    if price_side < -0.00005 and rsi_2 < 58.0:\n        buy_score += 1.0\n    if price_side > 0.00005 and rsi_2 > 42.0:\n        sell_score += 1.0\n    if order_flow < -0.25 and rsi_2 < 55.0:\n        buy_score += 1.0\n    if order_flow > 0.25 and rsi_2 > 45.0:\n        sell_score += 1.0\n\n    if buy_score >= 3.0 and buy_score > sell_score + 1.0:\n        return {'gemini_buy': 1.0}\n    if sell_score >= 3.0 and sell_score > buy_score + 1.0:\n        return {'gemini_sell': 1.0}\n    if rsi_2 < buy_rsi2 and mom1 >= 0.0:\n        return {'gemini_buy': 1.0}\n    if rsi_2 > sell_rsi2 and mom1 <= 0.0:\n        return {'gemini_sell': 1.0}\n\n    if ema_20 <= 0.0:\n        if rsi_2 < 38.0 and order_flow <= 0.15:\n            return {'gemini_buy': 1.0}\n        if rsi_2 > 62.0 and order_flow >= -0.15:\n            return {'gemini_sell': 1.0}\n\n    return {}",
    "name": "OrderFlow-RSI-Scalp-v1",
    "explanation": "This child mutates the parent into a more active order-flow scalp by using bid/ask trade-side imbalance, spread, and short-window momentum to shift RSI thresholds dynamically. It fires on frequent micro-reversions and momentum flips rather than waiting for extreme RSI only.",
    "parent_id": "7392b0b6"
  },
  "664bce9b": {
    "id": "664bce9b",
    "code": "def calculate_dynamic_signals(state, history):\n    symbol = 'BTC/USDT'\n    rsi_2 = state.signals.get(f'{symbol}_rsi_2', 50.0)\n    ema_20 = state.signals.get(f'{symbol}_ema_20', 0.0)\n\n    bars = history.get(symbol, [])\n    prices = []\n    for bar in reversed(bars):\n        price = None\n        for attr in ('close', 'last', 'bid', 'ask'):\n            value = getattr(bar, attr, None)\n            if value not in (None, 0):\n                price = float(value)\n                break\n        if price is not None:\n            prices.append(price)\n        if len(prices) >= 12:\n            break\n\n    last_price = prices[0] if prices else 0.0\n    ema_dist = 999.0\n    if ema_20 > 0 and last_price > 0:\n        ema_dist = abs(last_price - ema_20) / ema_20\n\n    micro_vol = 0.0\n    if len(prices) >= 5:\n        window = prices[:5]\n        mean = sum(window) / len(window)\n        micro_vol = (sum((x - mean) ** 2 for x in window) / len(window)) ** 0.5\n        if last_price > 0:\n            micro_vol /= last_price\n\n    short_pullback = len(prices) >= 4 and prices[0] < prices[1] and prices[0] <= prices[2]\n    short_rip = len(prices) >= 4 and prices[0] > prices[1] and prices[0] >= prices[2]\n    near_ema = ema_dist <= 0.014 or micro_vol >= 0.0022\n    vol_floor = 0.00045\n\n    buy = (rsi_2 <= 42.0 and near_ema and (micro_vol >= vol_floor or short_pullback or rsi_2 <= 30.0)) or (rsi_2 <= 28.0 and last_price > 0)\n    sell = (rsi_2 >= 58.0 and near_ema and (micro_vol >= vol_floor or short_rip or rsi_2 >= 70.0)) or (rsi_2 >= 72.0 and last_price > 0)\n\n    if buy:\n        return {'gemini_buy': 1.0}\n    if sell:\n        return {'gemini_sell': 1.0}\n    return {}\n",
    "name": "MicroVol-RSI2-Scalp-v1",
    "explanation": "This child widens the RSI-2 scalp bands to 42/58 and allows signals when price is near EMA20 or recent micro-volatility is elevated. It adds short pullback/rip confirmation from the last few ticks to catch quick mean-reversion flips more often than the parent.",
    "parent_id": "2bbb59ca"
  },
  "dbc2a5aa": {
    "id": "dbc2a5aa",
    "code": "def calculate_dynamic_signals(state, history):\n    symbol = 'BTC/USDT'\n    state_signals = getattr(state, 'signals', {}) or {}\n    history = history or {}\n\n    def to_float(value, default=0.0):\n        try:\n            if value is None:\n                return default\n            return float(value)\n        except (TypeError, ValueError):\n            return default\n\n    def pick_value(obj, names):\n        if obj is None:\n            return None\n        if isinstance(obj, dict):\n            for name in names:\n                if name in obj:\n                    value = obj[name]\n                    if isinstance(value, dict):\n                        value = value.get('price') or value.get('last') or value.get('vwap')\n                    if value is not None:\n                        try:\n                            return float(value)\n                        except (TypeError, ValueError):\n                            pass\n        else:\n            for name in names:\n                value = getattr(obj, name, None)\n                if isinstance(value, dict):\n                    value = value.get('price') or value.get('last') or value.get('vwap')\n                if value is not None:\n                    try:\n                        return float(value)\n                    except (TypeError, ValueError):\n                        pass\n        return None\n\n    rsi_2 = to_float(state_signals.get(symbol + '_rsi_2', 50.0), 50.0)\n    rsi_14 = to_float(state_signals.get(symbol + '_rsi_14', 50.0), 50.0)\n    ema_20 = to_float(state_signals.get(symbol + '_ema_20', 0.0), 0.0)\n\n    bars = history.get(symbol, [])\n    prices = []\n    bids = []\n    asks = []\n\n    for ticker in bars[-10:]:\n        price = pick_value(ticker, ('close', 'last', 'vwap', 'lastPrice', 'price'))\n        if price is not None and price > 0:\n            prices.append(price)\n\n        bid = pick_value(ticker, ('bid', 'bidPrice'))\n        ask = pick_value(ticker, ('ask', 'askPrice'))\n        if bid is not None and ask is not None and bid > 0 and ask > 0:\n            bids.append(bid)\n            asks.append(ask)\n\n    close = prices[-1] if prices else ema_20\n    if close <= 0:\n        close = ema_20 if ema_20 > 0 else 1.0\n\n    mid = 0.0\n    spread = 0.0\n    if bids and asks:\n        mid = (bids[-1] + asks[-1]) * 0.5\n        spread = (asks[-1] - bids[-1]) / mid if mid > 0 else 0.0\n\n    price_side = (close - mid) / mid if mid > 0 else 0.0\n\n    if len(prices) >= 4:\n        micro_mom = close - (sum(prices[-4:-1]) / 3.0)\n        micro_range = (max(prices[-5:]) - min(prices[-5:])) / max(sum(prices[-5:]) / len(prices[-5:]), 1e-9)\n    else:\n        micro_mom = 0.0\n        micro_range = 0.0001\n\n    micro_range = max(micro_range, 0.00005)\n\n    buy_rsi2 = 34.0\n    sell_rsi2 = 66.0\n    buy_rsi14 = 55.0\n    sell_rsi14 = 45.0\n\n    if ema_20 > 0.0 and close > 0.0:\n        ema_dev = (close - ema_20) / ema_20\n        if ema_dev > 0.0006:\n            sell_rsi2 = min(sell_rsi2, 62.0)\n            sell_rsi14 = min(sell_rsi14, 50.0)\n            buy_rsi2 = max(buy_rsi2, 38.0)\n            buy_rsi14 = max(buy_rsi14, 58.0)\n        elif ema_dev < -0.0006:\n            buy_rsi2 = min(buy_rsi2, 30.0)\n            buy_rsi14 = min(buy_rsi14, 50.0)\n            sell_rsi2 = max(sell_rsi2, 70.0)\n            sell_rsi14 = max(sell_rsi14, 58.0)\n\n    if spread > 0.0 and abs(price_side) <= spread * 0.50:\n        buy_rsi2 = min(buy_rsi2, 36.0)\n        sell_rsi2 = min(sell_rsi2, 64.0)\n    elif price_side < -spread * 0.35:\n        buy_rsi2 = min(buy_rsi2, 31.0)\n        buy_rsi14 = min(buy_rsi14, 54.0)\n    elif price_side > spread * 0.35:\n        sell_rsi2 = min(sell_rsi2, 62.0)\n        sell_rsi14 = min(sell_rsi14, 54.0)\n\n    if micro_mom < -micro_range * close * 0.15:\n        buy_rsi2 = min(buy_rsi2, 35.0)\n        buy_rsi14 = min(buy_rsi14, 56.0)\n    elif micro_mom > micro_range * close * 0.15:\n        sell_rsi2 = min(sell_rsi2, 63.0)\n        sell_rsi14 = min(sell_rsi14, 56.0)\n\n    if micro_range < 0.00030:\n        buy_rsi2 = min(buy_rsi2, 36.0)\n        sell_rsi2 = min(sell_rsi2, 64.0)\n\n    signals = {}\n\n    if rsi_2 <= buy_rsi2 or (rsi_2 <= buy_rsi2 + 4.0 and rsi_14 <= buy_rsi14 and (ema_20 <= 0.0 or close <= ema_20 * 1.0010)):\n        signals['gemini_buy'] = 1.0\n    elif rsi_2 >= sell_rsi2 or (rsi_2 >= sell_rsi2 - 4.0 and rsi_14 >= sell_rsi14 and (ema_20 <= 0.0 or close >= ema_20 * 0.9990)):\n        signals['gemini_sell'] = 1.0\n\n    return signals",
    "name": "Spread-Weighted-RSI-Scalp-v1",
    "explanation": "This child strategy mutates the parent by using tighter RSI bands and spread-relative price pressure to fire more often. It fades short-term micro-momentum when price is near the bid/ask side of the spread, aiming to scalp quick BTC/USDT mean reversion.",
    "parent_id": "7392b0b6"
  },
  "1444ce3a": {
    "id": "1444ce3a",
    "code": "def calculate_dynamic_signals(state, history):\n    symbol = 'BTC/USDT'\n\n    def to_float(x, default=None, reject_zero=False):\n        try:\n            v = float(x)\n        except (TypeError, ValueError):\n            return default\n        if v != v:\n            return default\n        if reject_zero and v == 0.0:\n            return default\n        return v\n\n    rsi_2 = to_float(state.signals.get(f'{symbol}_rsi_2', 50.0), 50.0)\n    ema_20 = to_float(state.signals.get(f'{symbol}_ema_20', 0.0), 0.0)\n    bars = history.get(symbol, [])\n\n    prices = []\n    bid_ask_flows = []\n\n    for bar in bars[-28:]:\n        price = None\n        for attr in ('last', 'close', 'bid', 'ask'):\n            price = to_float(getattr(bar, attr, None), None, True)\n            if price is not None:\n                break\n        if price is None:\n            continue\n\n        bid = to_float(getattr(bar, 'bid', None), None, True)\n        ask = to_float(getattr(bar, 'ask', None), None, True)\n\n        if bid is not None and ask is not None and ask > bid:\n            mid = (bid + ask) * 0.5\n            spread = ask - bid\n            bid_ask_flows.append(max(-1.0, min(1.0, (price - mid) / (spread * 0.5))))\n\n        prices.append(price)\n\n    if len(prices) < 4:\n        return {}\n\n    last_price = prices[-1]\n    prev_price = prices[-2]\n\n    price_flows = []\n    for i in range(1, len(prices)):\n        if prices[i - 1] > 0:\n            price_flows.append(max(-1.0, min(1.0, (prices[i] - prices[i - 1]) / prices[i - 1] * 10000.0)))\n\n    if bid_ask_flows:\n        recent_price_flow = price_flows[-len(bid_ask_flows):]\n        flow = (sum(bid_ask_flows) / len(bid_ask_flows) * 0.65) + (sum(recent_price_flow) / max(len(recent_price_flow), 1) * 0.35)\n    else:\n        recent_price_flow = price_flows[-12:]\n        flow = sum(recent_price_flow) / max(len(recent_price_flow), 1)\n\n    look6 = prices[-min(6, len(prices))]\n    look12 = prices[-min(12, len(prices))]\n    mom6_bps = ((last_price - look6) / look6) * 10000.0 if look6 > 0 else 0.0\n    mom12_bps = ((last_price - look12) / look12) * 10000.0 if look12 > 0 else 0.0\n    ema_gap_bps = ((ema_20 - last_price) / ema_20) * 10000.0 if ema_20 > 0 else 0.0\n\n    rets = []\n    for i in range(1, len(prices)):\n        if prices[i - 1] > 0:\n            rets.append((prices[i] - prices[i - 1]) / prices[i - 1])\n\n    vol_bps = 1.0\n    if rets:\n        avg_ret = sum(rets) / len(rets)\n        vol_bps = max((sum((r - avg_ret) ** 2 for r in rets) / len(rets)) ** 0.5 * 10000.0, 1.0)\n\n    score = 0.0\n    score += (50.0 - rsi_2) * 0.65\n    score += ema_gap_bps * 0.28\n    score += (-mom6_bps) * 0.20\n    score += (-mom12_bps) * 0.08\n    score += flow * 5.5\n    score += (1.0 if last_price > prev_price else -1.0 if last_price < prev_price else 0.0) * (vol_bps * 0.08)\n\n    active_threshold = max(3.5, 5.5 - vol_bps * 0.25)\n\n    if score >= active_threshold:\n        return {'gemini_buy': 1.0}\n    if score <= -active_threshold:\n        return {'gemini_sell': 1.0}\n    return {}",
    "name": "BidAsk-RSI-Momentum-Scalper",
    "explanation": "Mutates the parent from a simple RSI/EMA gate into a micro-flow score that blends RSI-2 mean reversion, EMA gap, short momentum fade, bid/ask trade-position pressure, and micro-volatility. This should fire frequently on BTC/USDT scalps because small overshoots near the short EMA are amplified by order-book pressure.",
    "parent_id": "2bbb59ca"
  },
  "27571449": {
    "id": "27571449",
    "code": "def calculate_dynamic_signals(state, history):\n    symbol = 'BTC/USDT'\n    if isinstance(state, dict):\n        state_signals = state.get('signals', {}) or {}\n    else:\n        state_signals = getattr(state, 'signals', {}) or {}\n    history = history or {}\n\n    def to_float(value, default=0.0):\n        try:\n            if value is None:\n                return default\n            return float(value)\n        except (TypeError, ValueError):\n            return default\n\n    def pick_value(obj, names):\n        if obj is None:\n            return None\n        if isinstance(obj, dict):\n            for name in names:\n                if name in obj:\n                    value = obj[name]\n                    if isinstance(value, dict):\n                        value = value.get('price') or value.get('last') or value.get('vwap')\n                    if value is not None:\n                        try:\n                            return float(value)\n                        except (TypeError, ValueError):\n                            pass\n        else:\n            for name in names:\n                value = getattr(obj, name, None)\n                if isinstance(value, dict):\n                    value = value.get('price') or value.get('last') or value.get('vwap')\n                if value is not None:\n                    try:\n                        return float(value)\n                    except (TypeError, ValueError):\n                        pass\n        return None\n\n    rsi_2 = to_float(state_signals.get(symbol + '_rsi_2', 50.0), 50.0)\n    rsi_14 = to_float(state_signals.get(symbol + '_rsi_14', 50.0), 50.0)\n    ema_20 = to_float(state_signals.get(symbol + '_ema_20', 0.0), 0.0)\n\n    bars = history.get(symbol, [])\n    prices = []\n    mids = []\n    for ticker in bars[-24:]:\n        price = pick_value(ticker, ('close', 'last', 'vwap', 'lastPrice', 'price'))\n        if price is not None:\n            prices.append(price)\n        bid = pick_value(ticker, ('bid', 'bidPrice'))\n        ask = pick_value(ticker, ('ask', 'askPrice'))\n        if bid is not None and ask is not None and bid > 0 and ask > 0:\n            mids.append((bid + ask) / 2.0)\n\n    close = prices[-1] if prices else ema_20\n    mid = mids[-1] if mids else 0.0\n    if mid <= 0.0 and close > 0.0:\n        mid = close\n    price_side = (close - mid) / mid if mid > 0.0 else 0.0\n\n    if len(prices) >= 3:\n        micro_mom = close - sum(prices[-3:]) / 3.0\n        window = prices[-8:]\n        vol = (max(window) - min(window)) / max(sum(window) / len(window), 1e-9)\n    else:\n        micro_mom = 0.0\n        vol = 0.0001\n    vol = max(vol, 0.0001)\n\n    if len(prices) >= 2:\n        tick_count = 0.0\n        for i in range(max(1, len(prices) - 8), len(prices)):\n            if prices[i] > prices[i - 1]:\n                tick_count += 1.0\n            elif prices[i] < prices[i - 1]:\n                tick_count -= 1.0\n        tick_pressure = tick_count / max(float(len(prices) - 1), 1.0)\n    else:\n        tick_pressure = 0.0\n\n    if len(mids) >= 3:\n        order_pressure = 0.0\n        for m in mids[-8:]:\n            if m > 0.0:\n                order_pressure += (close - m) / m\n        order_pressure /= max(len(mids[-8:]), 1)\n    else:\n        order_pressure = price_side\n\n    if ema_20 > 0.0 and close > 0.0:\n        ema_dev = (close - ema_20) / ema_20\n    else:\n        ema_dev = 0.0\n\n    buy_score = 0.0\n    sell_score = 0.0\n\n    if rsi_2 < 32.0:\n        buy_score += 2.3\n    elif rsi_2 < 43.0:\n        buy_score += 1.0\n    elif rsi_2 > 68.0:\n        sell_score += 2.3\n    elif rsi_2 > 57.0:\n        sell_score += 1.0\n\n    if rsi_14 < 52.0:\n        buy_score += 0.6\n    elif rsi_14 > 48.0:\n        sell_score += 0.6\n\n    if price_side < -0.00005:\n        buy_score += 0.8\n    elif price_side > 0.00005:\n        sell_score += 0.8\n\n    if order_pressure < -0.00004:\n        buy_score += 0.5\n    elif order_pressure > 0.00004:\n        sell_score += 0.5\n\n    mom_gate = vol * max(close, mid, 1e-9) * 0.07\n    if micro_mom < -mom_gate:\n        buy_score += 0.7\n    elif micro_mom > mom_gate:\n        sell_score += 0.7\n\n    if ema_dev < -0.00045:\n        buy_score += 0.5\n    elif ema_dev > 0.00045:\n        sell_score += 0.5\n\n    if tick_pressure < -0.20:\n        buy_score += 0.4\n    elif tick_pressure > 0.20:\n        sell_score += 0.4\n\n    if buy_score >= sell_score + 0.5 and buy_score >= 2.5:\n        return {'gemini_buy': 1.0}\n    if sell_score >= buy_score + 0.5 and sell_score >= 2.5:\n        return {'gemini_sell': 1.0}\n\n    if rsi_2 < 25.0 or (rsi_2 < 38.0 and price_side < 0.00002 and ema_dev <= 0.0003):\n        return {'gemini_buy': 1.0}\n    if rsi_2 > 75.0 or (rsi_2 > 62.0 and price_side > -0.00002 and ema_dev >= -0.0003):\n        return {'gemini_sell': 1.0}\n\n    return {}",
    "name": "Micro-Pressure-RSI-Scalp-v1",
    "explanation": "Mutation replaces fixed RSI gates with adaptive buy/sell scoring across RSI_2, RSI_14, EMA deviation, micro-momentum, bid-ask pressure, and tick pressure, producing more frequent BTC/USDT mean-reversion scalps.",
    "parent_id": "7392b0b6"
  }
}